#include "MeshIntersection.hpp"

/*
void JMeshIntersection :: setMesh(const JMeshPtr &m)
{
   mesh = m;
   if( mesh == nullptr) return ;
   JMeshIO mio;
   mio.saveAs(mesh, "tmp.off");

   std::ifstream input("tmp.off");
   CGALMesh cmesh;

   tree.reset( new Tree(faces(cmesh).first, faces(cmesh).second, cmesh));
}
*/
   
 

